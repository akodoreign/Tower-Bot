# Campaign Docs Placeholder

This folder is intentionally blank in git.

The live bot uses MySQL as the authoritative store for campaign data. Do not commit live campaign JSON, text logs, PDFs, generated images, extracted training images, or training source documents here.

Local-only reference/training material can be placed here on the machine running the bot when needed.
